		<% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
 <% end %>
 
		<%= div_for comment do %>
			<p >
				<div class="ctime">Posted <%= time_ago_in_words(comment.created_at)%> ago
				<%= link_to '(x)', [comment.post, comment], method: :delete, data: { confirm: 'Are you sure?' }, remote: true %>
				</div>
				<div class="cbody"><%= h(comment.body)%></div>
				
			</p>
		<% end %>